{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.8",
    "tailwindcss": "^4.1.8"
  },
    "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx @tailwindcss/cli -i input.css -o output.css",
    "build": "npx @tailwindcss/cli -i input.css -o output.css",
    "dev": "npx @tailwindcss/cli -i input.css -o output.css"
  },
  "devDependencies": {
    "daisyui": "^5.0.43",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.12"
  }
}

